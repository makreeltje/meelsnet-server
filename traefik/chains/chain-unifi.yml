http:
  middlewares:
    chain-unifi:
      chain:
        middlewares:
          - middlewares-rate-limit
          - middlewares-secure-headers
          - middlewares-unifi-headers
          - middlewares-oauth